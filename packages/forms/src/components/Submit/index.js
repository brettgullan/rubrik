import React from 'react'

import { Alpha } from '../../buttons'

// ----------------------------------------------------------------------------

const Submit = (props) => <Alpha as="button" type="submit" {...props} />

export default Submit
